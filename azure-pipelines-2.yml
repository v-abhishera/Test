trigger: none

pool:
  vmImage: windows-latest

steps:
- task: DownloadGitHubRelease@0
  inputs:
    connection: 'Test'
    userRepository: 'v-abhishera/Test'
    defaultVersionType: 'latest'
    downloadPath: '$(System.ArtifactsDirectory)'